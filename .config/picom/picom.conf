backend = "glx";
vsync = true;
# glx-use-copysubbuffer-mesa = true;

# glx-copy-from-front = true;
# glx-swap-method = 2;
# xrender-sync = true;
# xrender-sync-fence = true;

opacity-rule = [
    "90:class_g = 'Alacritty'",
    "90:class_g = 'code'"
];

blur:
{
    method = "gaussian";
    # size = 25; 
    # strength = 20;
    # deviation = 15;
};
